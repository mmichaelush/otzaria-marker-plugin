# תרומה לתוסף מרקר

המטרה: PR קטן, ברור ובטוח, שמתאים לאוצריא `0.9.97` ומעלה.

## דרישות מוקדמות

- Node.js 20 ומעלה (עבור `node:test` ו-`node --check`).
- אוצריא `0.9.97` או `0.9.98` מותקנת, לבדיקת אינטגרציה ולאריזה.
- Git.
- **אין `npm install`** — לתוסף אין תלויות, אין bundler ואין שלב build.

## התחלה מהירה

1. שכפלו או פצלו את המאגר וצרו branch ייעודי.
2. קראו את `docs/ARCHITECTURE.md` ואת `AGENTS.md`.
3. בצעו שינוי ממוקד.
4. הוסיפו או עדכנו בדיקות ביחס לסיכון.
5. הריצו את כל פקודות האימות.
6. ארזו עם אוצריא וּודאו שדו״ח ה-DESIGN_GUIDE נקי.

## מפת קבצים

| קובץ | תוכן |
|------|-------|
| `manifest.json` | זהות, גרסה, הרשאות, ותרומות עלייה דקלרטיביות |
| `index.html` | מבנה המסך בלבד — אין בו לוגיקה |
| `background.html` | מנוע הרקע: טעינת הסקריפטים בלבד, בלי DOM ובלי CSS |
| `css/style.css` | ערכת הממשק. הצבעים מגיעים מתפקידי ה-theme של אוצריא |
| `i18n/en.js` | קטלוג אנגלית. המפתחות הם מחרוזות המקור בעברית |
| `js/marker-domain.js` | פונקציות טהורות וחוזי נתונים |
| `js/marker-i18n.js` | שכבת התרגום |
| `js/marker-runtime.js` | גבול ה-SDK, שגיאות, לוגים, תור סדרתי |
| `js/marker-richtext.js` | עורך ההערות וה-sanitizer. נטען בדף בלבד |
| `js/marker-core.js` | המנוע: הגדרות, הדגשות, תרומות, גיבוי, דיווח, lifecycle |
| `js/marker-ui.js` | הדף: rendering, טפסים, דיאלוגים |
| `js/marker-background.js` | הפעלת המנוע ב-`background.html` ותו לא |
| `test/` | בדיקות Node ללא DOM וללא תלויות |
| `test/helpers/harness.js` | host מדומה למנוע |

## איפה לשים קוד חדש

שאלו: **האם זה חייב לעבוד כשדף התוסף סגור?**

- כן, ואפשר לבדוק בלי DOM ובלי SDK → `marker-domain.js`.
- כן, אבל צריך את ה-SDK → `marker-core.js`.
- לא, זה שייך למסך → `marker-ui.js`.
- זו קריאה חדשה ל-SDK → היא עוברת דרך `MarkerRuntime`, תמיד.

## כללי תאימות

1. יעד המארח הוא אוצריא `0.9.97` ומעלה.
2. `minAppVersion` = הגרסה שבה נוסף ה-API החדש ביותר שבשימוש. אין להעלות אותו
   בלי צורך — כל העלאה חוסמת התקנה אצל משתמשים בגרסה קודמת.
3. שימוש ב-API חדש = עדכון של המניפסט, של הטבלה ב-`test/compatibility.test.js`
   ושל ה-CI, בשלושתם יחד.
4. אין CDN, גופן מרוחק, analytics או הרשאת רשת.
5. classic scripts בלבד, בסדר הטעינה שב-`index.html`.
6. תפריט ההקשר נשאר שני פריטים עליונים בדיוק: `marker-colors` ו-
   `marker-highlight-actions` (שבתוכו `marker-note` ו-`marker-remove`).
   ה-Host אוכף תקרה של שניים לכל תוסף.

## כללי לוגיקה

- כל `settings` עוברים `normalizeSettings`; כל רשומת highlight עוברת
  `normalizeHighlight`.
- מזהה highlight עובר `isSafeHighlightId` לפני שהוא הופך למפתח אחסון.
- handler של אירוע נרשם דרך `MarkerRuntime.on` (עוטף ב-`protectEvent`).
- פעולה שמשנה מצב ב-host עוברת בתור (`MarkerRuntime.createQueue`) — מגביל הקצב
  של אוצריא הוא דלי של 50 אסימונים.
- אסור למחוק שדה קיים מרשומת highlight בלי migration ובדיקת שחזור.

## כללי UI

- רק תפקידי צבע: `--color-surface-container`, `--color-primary`,
  `--color-outline-variant` וכו׳. אין hex בגוף ה-CSS.
- רק משתני radius מוגדרים. אין `border-radius` בפיקסלים.
- אייקונים: SVG מקומי בשפת Fluent System Icons. אין תלות רשת.
- select חדש עובר דרך `.otz-select`; ה-select המקורי נשאר מקור הערך.
- רכיבים דינמיים חייבים לעבוד ב-280px, בדסקטופ ובמצב כהה.
- כל טקסט חדש מקבל `data-i18n` (או `t()` בקוד) **ותרגום ב-`i18n/en.js`**.
- כפתור אייקון ללא טקסט חייב `aria-label` מתורגם
  (`data-i18n-attr="aria-label"`).

## שינוי סכמת נתונים

1. ברירת מחדל ב-`DEFAULT_SETTINGS` או במסלול יצירת הרשומה.
2. נרמול לאחור ב-`normalizeSettings` / `normalizeHighlight`.
3. בדיקה לנתון חסר, ישן ולא תקין.
4. עדכון `parseBackup` — קבצי גיבוי ישנים חייבים להמשיך להיטען.
5. תיעוד ב-`docs/ARCHITECTURE.md`.

## בדיקות חובה

```text
for f in js/*.js i18n/*.js; do node --check "$f"; done
node --test "test/*.test.js"
git diff --check
```

ה-CI מריץ בדיוק את אלה לפני שהוא שוקל לפרסם — כולל קובצי הבדיקה החדשים,
כי הדפוס `test/*.test.js` תופס את כולם.

## בדיקות ידניות באוצריא

בדיקות Node אינן מריצות DOM. לפני שחרור, בתוך אוצריא:

**סימון**
- סימון בבחירה רגילה, בבחירת צורת הדף, ובבחירה שחוצה כמה פסקאות.
- סימון מעל הדגשה קיימת (החלפה).
- לחיצה ימנית על הדגשה קיימת → "הסר סימון".
- הסרת חלק מבחירה רב-פסקתית — כל החלקים נעלמים יחד.
- ניסיון סימון ב-PDF → הודעה מסבירה, לא כשל שקט.

**המסך**
- פתיחה, עריכה, שינוי צבע, העתקה ומחיקה מרשימת ההדגשות; ביטול מחיקה.
- חיפוש, סינון, מיון, קיבוץ, פס הסינון המהיר ופעולות מרוכזות.
- ניווט מקלדת ברשימה: חצים, `Home`, `End`, רווח, `Enter`.
- ניהול תגיות: שינוי שם, מיזוג, מחיקה.
- שינוי כל סוגי הצבע והסגנון, וסידור מחדש בגרירה.
- מיון וקיבוץ נזכרים אחרי סגירה ופתיחה; המסננים לא.

**עורך ההערות**
- כל כלי בסרגל, כולל על בחירה שחוצה כמה פסקאות בתוך ההערה.
- `Ctrl+Enter` שומר; `Ctrl+B/I/U` מעצבים.
- הדבקה מ-Word ומדפדפן — העיצוב הנתמך נשמר, השאר מנוקה.
- קישור: כתובת תקינה נוספת, כתובת פסולה נדחית עם הודעה.
- סגירה ב-Escape עם הערה שלא נשמרה — מוצגת שאלה.
- סגירת כרטיסיית התוסף עם הערה שלא נשמרה — אוצריא שואלת.
- הערה מעוצבת מוצגת נכון בכרטיס, בייצוא ל-HTML ובהדפסה.
- פתיחת הערה ישנה (טקסט בלבד) שנוצרה לפני 0.9.5.

**הדפסה**
- `הדפסה` ו-`ייצוא ל-PDF` עם יותר מ-100 הדגשות — כולן חייבות להופיע.
- הפעלה מתוך סינון פעיל — רק המסוננות מודפסות, והכותרת מציינת את הספר.

**מחזור חיים**
- הפעלה מחדש של אוצריא — ההדגשות חוזרות לספרים **בלי** לפתוח את לשונית התוסף.
- לחיצה על צבע בתפריט לפני שהתוסף נפתח מעולם באותה הרצה — הסימון מתבצע בספר,
  והלשונית אינה נפתחת.
- ספרייה של יותר מ-50 סימונים, ואז עלייה קרה — כולם חייבים לחזור לספר. זו
  התקרה של מגביל הקצב, ומתחתיה הבאג לא מתגלה.
- כיבוי `app.startup_contributions` — מוצגת הודעה מתאימה בדף.
- כיבוי `reader.highlight` — מוצגת הודעה מתאימה, וההדגשות עדיין נשמרות.
- שינוי הרשאה תוך כדי ריצה — התרומות נרשמות מחדש והסימונים מצוירים מחדש.
- שינוי צבע בדף — התפריט מתעדכן מיד, והעדכון שורד את סגירת הדף.

**שני חלונות**
- סימון בחלון א׳ — מופיע בחלון ב׳ תוך כשתי שניות. כך גם מחיקה, שינוי צבע
  והסתרת ספר.
- סגירת חלון ב׳ — הסימונים בחלון א׳ אינם זזים.

**שפה**
- מעבר לאנגלית בהגדרות אוצריא, ובחירה ידנית בהגדרות התוסף.
- שמות הפריטים בתפריט הלחיצה הימנית מתורגמים גם הם.
- כיוון הטקסט מתהפך, וה-HTML הסטטי נשאר `dir="rtl"`.

**נתונים**
- ייצוא לקריאה בשלושת הפורמטים; ייצוא וייבוא גיבוי (מיזוג והחלפה).
- שחזור מגיבוי אוטומטי.
- טעינת קובץ גיבוי מגרסה 0.9.4.

**תצוגה**
- מצב בהיר וכהה; רוחבים 280, 390, 680 ו-1180 פיקסלים לפחות.

## אריזה

```powershell
& 'C:\Program Files\otzaria\otzaria.exe' pack-plugin `
  'C:\path\to\otzaria-marker-plugin' `
  --output 'marker.otzplugin' `
  --force
```

האריזה חייבת להציג:

```text
✓ העיצוב תואם לתיעוד (DESIGN_GUIDE).
```

אזהרות על הרשאות בסיס שמאושרות אוטומטית מ-0.9.97 ואילך צפויות ואינן חוסמות.

## תוכן PR

- תיאור הבעיה וההתנהגות הצפויה.
- פירוט הקבצים והחוזים שהשתנו.
- תוצאות הבדיקות האוטומטיות, ואילו בדיקות ידניות בוצעו בפועל.
- צילומי מסך לפני/אחרי לשינוי UI.
- הצהרה על כל API חדש ועל הגרסה שבה נוסף.
- הערות migration או rollback כשנתונים משתנים.
